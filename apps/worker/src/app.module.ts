import { Module } from '@nestjs/common';

// Deliberately empty. Plan 05 (Backend API Foundation) and the later queue
// plans (Plan 24+) decide how much of apps/api's module structure worker
// actually shares vs. duplicates — this plan just reserves the shape.
@Module({})
export class AppModule {}
